---
name: project-status
description: Зведення по проєкту Hero Armor — відкриті задачі по саб-проєктах, замовлення в дорозі, ship-to LA→SF, що купити. Викликати на "де ми", "статус", "що далі".
---

Побудуй зведення проєкту:

1. Прочитай `data/tasks.json`, `orders.json`, `bom.json`, `addresses.json`.
2. Запусти `cd model && python3 audio_node_model.py` — свіжі числа моделі.
3. Звіт у чат (стисло, по-людськи):
   - Задачі по саб-проєктах: doing → waiting (що їх розблокує) → топ-3 todo.
   - Замовлення: що в дорозі, скільки днів, куди; що вже можна тестувати.
   - Ship-to: скільки днів лишилось слати на LA (якщо move_date задана).
   - Закупівля: скільки позицій `add`/`tbd` і орієнтовна сума.
   - Один головний ризик тижня (наприклад: динаміки не приїхали → A/B тест
     блокує прошивку гучності).
4. Нічого не редагуй — це read-only звіт.
